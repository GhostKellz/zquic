# Resolved Advisories

No resolved security advisories are currently tracked for `zquic v0.9.17`.

When a dependency or code update clears an advisory, record:

- advisory identifier
- affected dependency or module
- fixed version or commit
- release that shipped the fix
- verification command or test that confirmed the fix

Security-relevant release notes should also be reflected in `CHANGELOG.md`.
